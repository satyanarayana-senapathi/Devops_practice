
name: Deploy to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 14
      
#       - name: Login to Docker Hub
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v2
#         with:
#           context: .my-app
#           push: true
#           tags: satyanarayanasvv/react-app:${{ github.sha }}
#           dockerfile: Dockerfile
        
      - name: SSH into EC2 instance and pull Docker image
        run: |
            ssh -o StrictHostKeyChecking=no -i ${{ secrets.secrets.EC2_KEY }} ubuntu@ec2-3-111-168-160.ap-south-1.compute.amazonaws.com           
