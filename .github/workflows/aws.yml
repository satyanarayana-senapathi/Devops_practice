name: Deploy to AWS EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14
      
#       - name: Login to Docker Hub
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v2
#         with:
#           context: .
#           push: true
#           tags: satyanarayanasvv/react-app:${{ github.sha }}
#           dockerfile: Dockerfile

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v1
#         with:
# #           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.EC2_PRIVATE_KEY }}
#           aws-region: ap-south-1
      - name : create private key file
        run: |
          pwd
          echo ${{ secrets.EC2_PRIVATE_KEY }} > ~/.ssh/key.pem
          chmod 400 ~/.ssh/key.pem
        
      - name: SSH into EC2 instance and pull Docker image
        run: |
            ssh -o StrictHostKeyChecking=no -i ~/.ssh/key.pem ubuntu@ec2-13-233-116-175.ap-south-1.compute.amazonaws.com            
            sudo apt install nginx -y
